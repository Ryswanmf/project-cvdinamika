<?= $this->include('landing-page/layout/header') ?>

<style>
    /* Filter Styles */
    .portfolio-filters {
        list-style: none;
        padding: 0;
        margin: 0 0 50px 0;
        text-align: center;
        display: flex;
        justify-content: center;
        flex-wrap: wrap;
        gap: 15px;
    }

    .portfolio-filters li {
        cursor: pointer;
        padding: 10px 25px;
        background: #f8f9fa;
        color: #666;
        border-radius: 50px;
        font-weight: 600;
        font-size: 0.95rem;
        transition: all 0.3s ease;
        border: 1px solid #eee;
    }

    .portfolio-filters li:hover,
    .portfolio-filters li.filter-active {
        background: #14756E;
        color: #fff;
        box-shadow: 0 5px 15px rgba(20, 117, 110, 0.2);
        border-color: #14756E;
    }

    /* Masonry Grid */
    .projects-container {
        position: relative;
        min-height: 500px; /* Prevent collapse during loading */
    }

    .project-item {
        width: 33.333%;
        padding: 15px;
        opacity: 0;
        animation: fadeInUp 0.8s ease-out forwards;
    }
    
    /* Staggered animation delays */
    .project-item:nth-child(1) { animation-delay: 0.1s; }
    .project-item:nth-child(2) { animation-delay: 0.2s; }
    .project-item:nth-child(3) { animation-delay: 0.3s; }
    .project-item:nth-child(4) { animation-delay: 0.4s; }
    .project-item:nth-child(5) { animation-delay: 0.5s; }
    .project-item:nth-child(6) { animation-delay: 0.6s; }
    .project-item:nth-child(7) { animation-delay: 0.7s; }
    .project-item:nth-child(8) { animation-delay: 0.8s; }
    .project-item:nth-child(9) { animation-delay: 0.9s; }
    .project-item:nth-child(n+10) { animation-delay: 1s; }
    
    @keyframes fadeInUp {
        from {
            opacity: 0;
            transform: translateY(30px);
        }
        to {
            opacity: 1;
            transform: translateY(0);
        }
    }
    
    .project-card {
        background: #ffffff;
        border-radius: 12px;
        padding: 0;
        border: 1px solid #e8f4f3;
        transition: transform 0.4s cubic-bezier(0.4, 0, 0.2, 1), box-shadow 0.4s ease;
        overflow: hidden;
        height: 100%;
        box-shadow: 0 5px 15px rgba(0,0,0,0.03);
    }
    
    .project-card:hover {
        transform: translateY(-8px);
        box-shadow: 0 20px 40px rgba(20, 117, 110, 0.2);
    }
    
    .project-image {
        width: 100%;
        height: 220px;
        overflow: hidden;
        position: relative;
    }
    
    .project-image::before {
        content: '';
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        background: linear-gradient(135deg, rgba(20, 117, 110, 0.1) 0%, transparent 100%);
        opacity: 0;
        transition: opacity 0.4s ease;
        z-index: 1;
    }
    
        position: relative;
    }
    
    .project-category {
        font-size: 0.8rem;
        text-transform: uppercase;
        letter-spacing: 1px;
        color: #14756E;
        font-weight: 700;
        margin-bottom: 5px;
        opacity: 0.9;
        transition: all 0.3s ease;
    }
    
    .project-card:hover .project-category {
        opacity: 1;
        letter-spacing: 1.5px;
    }
    
    .project-name {
        font-family: 'Montserrat', sans-serif;
        font-size: 1.15rem;
        font-weight: 700;
        color: #2c3e50;
        margin-bottom: 12px;
        line-height: 1.4;
        transition: color 0.3s ease;
    }
    
    .project-card:hover .project-name {
        color: #14756E
    
    .project-category {
        font-size: 0.8rem;
        text-transform: uppercase;
        letter-spacing: 1px;
        color: #14756E;
        font-weight: 700;
        margin-bottom: 5px;
    }
    
    .project-name {
        font-family: 'Montserrat', sans-serif;
        font-size: 1.15rem;
        font-weight: 700;
        color: #2c3e50;
        opacity: 0.85;
        transition: all 0.3s ease;
    }
    
    .project-card:hover .project-details-item {
        opacity: 1;
        transform: translateX(3px);
    }
    
    .project-details-item i {
        color: #14756E;
        margin-right: 8px;
        margin-top: 4px;
        font-size: 0.8rem;
        transition: transform 0.3s ease;
    }
    
    .project-card:hover .project-details-item i {
        transform: scale(1.1)
    }
    
    .project-details-item {
        display: flex;
        align-items: flex-start;
        margin-bottom: 5px;
    }
    
    .project-details-item i {
        color: #14756E;
        margin-right: 8px;
        margin-top: 4px;
        font-size: 0.8rem;
    }

    /* Page Title */
    .page-title-box {
        background: linear-gradient(135deg, rgba(20, 117, 110, 0.95) 0%, rgba(26, 145, 135, 0.95) 100%), url('/img/hero-bg.jpg');
        background-attachment: fixed;
        background-size: cover;
        background-position: center;
        color: white;
        padding: 100px 0 80px;
        margin-bottom: 0; /* Remove margin bottom for seamless transition */
        position: relative;
        overflow: hidden;
    }
    
    .page-title-main {
        font-family: 'Cormorant Garamond', serif;
        font-size: 3.5rem;
        font-weight: 700;
        color: white;
    }

    @media (max-width: 992px) {
        .project-item { width: 50%; }
    }
    @media (max-width: 768px) {
        .project-item { width: 100%; }
        .portfolio-filters { gap: 10px; }
        .portfolio-filters li { padding: 8px 15px; font-size: 0.85rem; }
    }
</style>

<!-- Page Header Start -->
<div class="page-title-box">
    <div class="container">
        <div class="row">
            <div class="col-12 text-center">
                <h1 class="page-title-main animated slideInDown">Hasil Instalasi</h1>
                <p class="mb-0 text-white-50">Portofolio Proyek Instalasi Lantai Berkualitas</p>
                <nav aria-label="breadcrumb" class="mt-4">
                    <ol class="breadcrumb justify-content-center mb-0 bg-transparent">
                        <li class="breadcrumb-item"><a href="<?= base_url('/') ?>" class="text-white text-decoration-none">Beranda</a></li>
                        <li class="breadcrumb-item text-white active" aria-current="page">Hasil Instalasi</li>
                    </ol>
                </nav>
            </div>
        </div>
    </div>
</div>

<div class="container-fluid py-5 bg-white">
    <div class="container py-5">
        
        <!-- Section Header -->
        <div class="text-center mx-auto mb-5 wow fadeInUp" data-wow-delay="0.1s" style="max-width: 600px;">
            <h2 class="mb-4 text-dark" style="font-family: 'Cormorant Garamond', serif; font-size: 2.5rem;">Proyek Kami</h2>
            <p class="text-muted">Lihat berbagai proyek instalasi lantai yang telah kami selesaikan di berbagai sektor, mulai dari rumah sakit hingga area olahraga.</p>
        </div>

        <!-- Filters -->
        <div class="row wow fadeInUp" data-wow-delay="0.3s">
            <div class="col-12 text-center">
                <ul class="portfolio-filters" id="portfolio-flters">
                    <li class="filter-active" data-filter="*">Semua</li>
                    <?php 
                        // Get unique categories
                        $uniqueCategories = array_unique(array_column($projects, 'category'));
                        foreach ($uniqueCategories as $cat):
                            if (empty($cat)) continue;
                            $filterClass = url_title($cat, '-', true);
                    ?>
                        <li data-filter=".filter-<?= $filterClass ?>"><?= $cat ?></li>
                    <?php endforeach; ?>
                </ul>
            </div>
        </div>

        <!-- Projects Container -->
        <div class="row projects-container wow fadeInUp" data-wow-delay="0.5s">
            <?php foreach ($projects as $project): 
                $catClass = url_title($project['category'], '-', true);
            ?>
            <div class="project-item filter-<?= $catClass ?>">
                <div class="project-card">
                    <div class="project-image">
                        <img src="<?= base_url('uploads/projects/' . $project['image']) ?>" alt="<?= esc($project['title']) ?>">
                    </div>
                    <div class="project-content">
                        <div class="project-category"><?= esc($project['category']) ?></div>
                        <h4 class="project-name"><?= esc($project['title']) ?></h4>
                        
                        <?php if (!empty($project['product_details'])):
                            // Tampilkan max 2 detail saja agar rapi
                            $details = explode("\n", strip_tags($project['product_details'])); // strip tags karena summernote
                            $details = array_filter($details, function($val) { return !empty(trim($val)); });
                            $count = 0;
                            foreach ($details as $detail): 
                                if($count >= 2) break;
                        ?>
                            <div class="project-details-item">
                                <i class="fas fa-check-circle"></i> <span><?= esc(trim($detail)) ?></span>
                            </div>
                        <?php 
                            $count++;
                            endforeach; 
                        endif; ?>
                    </div>
                </div>
            </div>
    window.addEventListener('load', function() {
        var portfolioIsotope = new Isotope('.projects-container', {
            itemSelector: '.project-item',
            layoutMode: 'fitRows'
        });

        // Filter items on button click with smooth animation
        var portfolioFilters = document.querySelectorAll('#portfolio-flters li');
        
        portfolioFilters.forEach(function(filterBtn) {
            filterBtn.addEventListener('click', function() {
                // Remove active class from all
                portfolioFilters.forEach(btn => btn.classList.remove('filter-active'));
                // Add active class to clicked
                this.classList.add('filter-active');
                
                // Get filter value
                var filterValue = this.getAttribute('data-filter');
                
                // Reset animation for filtered items
                var items = document.querySelectorAll('.project-item');
                items.forEach(function(item, index) {
                    item.style.animation = 'none';
                    setTimeout(function() {
                        item.style.animation = '';
                        item.style.animationDelay = (index * 0.1) + 's';
                    }, 10);
                });
                
                portfolioIsotope.arrange({ 
                    filter: filterValue,
                    transitionDuration: '0.6s'
               
        var portfolioFilters = document.querySelectorAll('#portfolio-flters li');
        
        portfolioFilters.forEach(function(filterBtn) {
            filterBtn.addEventListener('click', function() {
                // Remove active class from all
                portfolioFilters.forEach(btn => btn.classList.remove('filter-active'));
                // Add active class to clicked
                this.classList.add('filter-active');
                
                // Get filter value
                var filterValue = this.getAttribute('data-filter');
                portfolioIsotope.arrange({ filter: filterValue });
            });
        });
    });
</script>